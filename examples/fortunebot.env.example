# Copy this to `fortunebot.env` (gitignored) and fill in your secrets.
# Recommended: store API keys here or in real environment variables, not in config.json.
# These are loaded automatically via python-dotenv.
OPENAI_API_KEY=your_api_key_here
OPENAI_MODEL=gpt-4o-mini
